# White-background final visualizations

These selected Steps 75–81 scripts generated the audited white-background figure
set. They preserve black data marks where scientifically meaningful; "white
background" refers to figure canvas, axes, and exported transparency handling.

The scripts retain historical project paths. Point them to a validated local data
bundle and write outputs to a new directory so original results remain untouched.
